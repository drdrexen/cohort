const initArray = [1, 2, 3, 4, 5]
initArray.push(6);
console.log(initArray);
// pop -> deletes form the end
initArray.pop();
console.log(initArray);
// shift -> deletes fromt the start
initArray.shift();
console.log(initArray);

console.log("concat");
//concat
arr1 = [1, 2, 3, 4]
arr2 = [5, 6, 7, 8]
console.log(arr1.concat(arr2));

//push-> second element is the number
// concat-> second element is the array
